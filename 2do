* signatures ?
* timestamps ?

* httpd:
    clean it up
    separate handlers for per-file etc
    raw block download

* file head:
    include blockref lists into the head itself ?

* ref:
    inline data (no oid, but data)

* file reader:
    * support ranges
    * separate per-block reader objects
    * background fetch (goroutines)
