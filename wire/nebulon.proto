syntax = "proto3";

package wire;

enum RefType {
    Blob = 0;
    RefList = 1;
}

message BlockRef {
    uint32 type = 1;
    bytes data = 2;
}

message OIDRefList {
    string magic = 1;
    repeated BlockRef refs = 2;
}
